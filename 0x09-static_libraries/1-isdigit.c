#include "main.h"

/**
 * _isdigit - Checks if a given character is a digit (0 through 9).
 * @s: The character to be checked.
 * Return: 1 if c is a digit, 0 otherwise
 */
int _isdigit(int s)
{
	return (s >= '0' && s <= '9');
}
